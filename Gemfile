source "https://rubygems.org"

# Matches the exact Jekyll version GitHub Pages runs, so local
# previews look the same as the deployed site.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
